# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.egg-info/

# Secrets / env — live secrets are NEVER committed (B3; CLAUDE.md never-do)
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Data / build artifacts
*.duckdb
*.parquet
*.sqlite
*.db
dist/
build/

# Local Claude overrides (the shared harness is .claude/settings.json)
.claude/settings.local.json

# B1a.1b cold-store data (reproducible from scripts/ingest_cold_store.py)
data_cold/

# sealed research store (discovery reads) + holdout store (gate-only) — scripts/seal_cold_store.py
data_research/
data_holdout/
data_cold_ticks/
data_research_ticks/
data_holdout_ticks/

# M3.0 funding history (scripts/ingest_funding.py) + M5.5 option chains (scripts/ingest_nse_fo.py)
data_funding/
data_options/
data_research_nge/
data_cold_flows/
data_research_flows/
data_holdout_flows/

# B1b.4 nightly-discovery per-run JSON records (syncable to the pod discovery_runs table)
discovery_runs/

# local git worktrees (parallel-work checkouts)
.claude/worktrees/

# Worker runtime state (dbs, heartbeats, instrument caches) — never committed
var/

# Local demo/runbook harnesses (kept out of the repo; see PR #157/#159 reviews)
config/demo.yaml
scripts/demo_trade_sync.py
