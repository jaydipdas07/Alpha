# TASKS.md — Alpha work queue

> **The work queue for the whole build.** `docs/DESIGN_v4.md` (canonical design) and `BUILD_PLAN.md`
> (v3 backbone + R1–R14 hardening) say *how* and *why*; `CLAUDE.md` is the operating contract; **this
> file says what's done and what's next.** The single live worklist is the **⏳ Pending tracker at the
> very end of this file** — everything above it is the planned record. Phases 0–1b are decomposed to
> executable tasks; Phases 2–6 are milestone work-packages, expanded to `B`-tasks on arrival.

**Status:** ✅ done · 🔶 in progress · ⏳ open (pending) · ⛔ out of scope · ➡️ superseded.
**Owners:** `[CC]` Claude Code builds · `[You]` keys / money / regulator / live-gate · `[You]+[CC]` you open the door, CC drives.
**ID scheme:** `B<phase>.<seq>` build task · `M<phase>.<seq>` milestone · `<phase>.GATE` gate · `TEST-<n>` cardinal invariant · `R1–R14` v3 hardening · `E1–E5 / B1–B5 / D1–D2` locked decisions.

## Phase index (status at a glance)

| Phase | What | Owner | Status |
|---|---|---|---|
| **0** | Foundation + **engine bake-off** (Nautilus-shell vs lift-Vega) → engine chosen | [CC]/[You] | ✅ (0.GATE, ADR 0001) |
| **1a** | Rigor crown-jewel + data plane (+ population calibration) | [CC] | ✅ (1a.GATE, ADR 0002) |
| **1b** | AI discovery — constrained track (strategist + quant-analyst + Workflow A) | [CC] | ✅ (1b.GATE, ADR 0003) |
| **2** | Cockpit + copilot + Telegram surface | [CC] | 🔶 built + DEPLOYED (M2.1–M2.5 ✅); **Telegram desk surface LIVE + verified on the dedicated Alpha bot (#107)**; 2.GATE pending [You] (ratify + daemon-persistence) |
| **3** | Paper + approval gate + worker (paper) + **deadman** | [CC]/[You] | 🔶 worker BUILT + DEPLOYED + RUNNING on **Delta perp testnet** (M3.1–M3.3/M3.5/M3.6 ✅, #94–#103); **3.GATE deadman drill ✅ live**; next = Workflow B (M3.7) + pod-sync |
| **4** | First real money — Delta live (staged) + start SEBI long-poles | [You]+[CC] | ⏳ |
| **5** | Indian equities/index live + multi-venue breadth | [You]+[CC] | ⏳ |
| **6** | Free-form code-gen (sandboxed) + scale | [CC]/[You] | ⏳ |

---

## Phase 0 — Foundation + Engine Bake-off · [CC] builds, [You] keys/AWS/Lemma
Goal: a correct `alpha-core`/`worker`/`pod` skeleton that lints/types/tests green; Lemma + money verified; the two-track engine bake-off run and the engine chosen. Proves **TEST-1**.

| ID | Task | Owner | Done-when |
|---|---|---|---|
| B0.1 | Scaffold `alpha-core/` + `worker/` + `pod/` + `docs/`; `pyproject.toml` (uv, py≥3.12; pydantic/pydantic-settings/structlog + dev ruff/mypy/pytest/hypothesis/pre-commit); ruff + `mypy --strict` + pytest config; CI (uv→ruff→mypy→pytest) | [CC] | ✅ green in CI; ruff + mypy clean; merged (#5) |
| B0.2 | Mirror Vega quality gates into `alpha-core`: `fail_under=94`, `mypy strict`, pre-commit hooks | [CC] | ✅ gates enforced locally + in CI (#7) |
| B0.3 | Lemma Phase-0 verification (B5): inspect money-column types → lock native DECIMAL vs string-Decimal; confirm `lemma` → Vault; verify table/RLS/FK primitives | [You]+[CC] | ✅ B5 = **TEXT string-Decimal** (Lemma has no native DECIMAL; FLOAT lossy + INTEGER is int32 — both proven); `Decimal` round-trips; `lemma`→Vault ok |
| B0.4 | Pod skeleton: `pod.json` + table DDL (deployments, strategies, backtests, discovery_runs, paper_runs, orders, fills, positions, pnl_snapshots, risk_events, **commands**(+`emergency_flatten`), **worker_status**, **research_status**, **research_ledger** keyed by (market,family,window), broker_credentials RLS) + seed | [CC] | ✅ 15 tables imported + seeded; FK JOIN + money round-trip + idempotency + RLS verified (#11) |
| B0.5 | AWS prep: SSH; preserve `.env` keys → Alpha gitignored secrets; wipe the AWS box's Vega **deployment** (keep the Mac Vega repo as the lift source); set up the research host | [You]+[CC] | ✅ research host `3.6.176.133` reachable; alpha-core runs (40 tests, aarch64); Vega stopped (kept, reversible); keys preserved; nothing live (#13) |
| B0.6 | `research_status` heartbeat table + `request_backtest` lease/timeout scaffolding (R8) | [CC] | ✅ lease + heartbeat-staleness logic (`alpha_core/research`); a dead box's work is reclaimable, never hangs; 100% cov (#12) |
| B0.7 | Portable strategy contract in `alpha-core`: `(bars, params) -> signals` (= Vega `Strategy` ABC) + a trivial MA-crossover reference impl | [CC] | ✅ contract + MA-crossover ref impl; 29 tests, 100% cov (#8) |
| B0.8 | **Track A (Nautilus-shell):** install NautilusTrader; wrap the contract in a Nautilus strategy; wire the Vega-pattern Indian cost model (+funding) into Nautilus's fee hook; Nautilus backtest + Delta-testnet live-paper via ccxt | [CC] | ✅ the B0.7 contract runs through a Nautilus backtest (#24, `track-a/`) with Vega's `CostModel` wired into a custom `FeeModel`; same 2 fills as Track B, net P&L matches to 8 decimals. (Live-testnet on Track A not pursued — a TTM data point; Track B is the recommendation, see 0.GATE.) |
| B0.9 | **Track B (lift-Vega):** copy Vega `core`/`execution`/`risk`/`backtest` into `alpha-core` (rename `tradebot`→`alpha_core`); run the same contract + Delta-testnet live-paper + backtest through the lifted engine | [CC] | ✅ **engine lifted** (core+risk+execution+strategy-engine+backtest/portfolio, #14–#18) **+ B0.7 contract proven through it**: deterministic backtest (#19) **and crypto-testnet live-paper** (#21 CcxtAdapter, #22 run — MaCrossover placed+filled a real order via StrategyEngine→OMS→CcxtAdapter on **Binance** testnet; Delta swapped to Binance per [You], no live/test dual accounts). 401 tests, ≥94% cov; `docs/bakeoff_b0_9f.md` |
| B0.10 | Parity + oracle: assert backtest ≡ paper parity on a replay per track (**TEST-1**); stand up the Vega differential-oracle (its property/replay suite) cross-checking P&L/positions; measure integration friction + time-to-code | [CC] | ✅ parity asserted — both tracks net the **same P&L to 8 decimals** on the same contract + cost kernel; the Vega differential oracle (property/replay suites) is in `alpha-core/tests` (Track B); comparison written as ADR 0001 |
| **0.GATE** | **Engine decision:** pick Track A or B on parity + integration friction + TTM (evidence favors B); record as `docs/adr/0001-engine-bakeoff.md` | [You]+[CC] | ✅ **[You] ratified Track B** (lifted Vega) on 2026-06-26 — ADR 0001 *accepted*; `track-a/` archived; `alpha-core` locked as the Phase-0 engine. Follow-up: `vega`→`alpha` namespacing PR. |

---

## Phase 1a — Rigor crown-jewel + data plane · [CC]
Goal: a trusted single backtester with full statistical rigor + population calibration. No money/keys. Proves **TEST-3**.

| ID | Task | Owner | Done-when |
|---|---|---|---|
| B1a.1 | Free-data ingest (Binance WS/historical, Kite ₹500 historical, NSE bhavcopy, Dukascopy) → Parquet/DuckDB cold store | [CC] | ✅ **B1a.1a** (#28) Parquet/DuckDB `BarStore` (Decimal-exact, idempotent/reproducible, DuckDB analytics) + lifted `data/{normalize,historical}` readers; **B1a.1b** (#30) Binance BTC-perp 5m + NIFTY-constituent (RELIANCE) daily **fetchers** (`data/ingest/` pure transforms + `scripts/ingest_cold_store.py`) — **289 + 21 bars loaded reproducibly** into the store (the initial seed; later expanded to **~18.8k bars / 6 series** at #73, then sealed at #75). |
| B1a.2 | Lift Vega cost model (Indian stack + TDS) into the chosen engine; **add perp funding accrual → P&L + risk gate** (R13) | [CC] | ✅ (#32) cost model already lifted (`execution/costs.py`); **perp funding added** — `execution/funding.py` + `OMS.accrue_funding` (into P&L **and** `_day_realized`) + `run_backtest` funding loop. Proven: funding shows in backtest P&L **and** a funding-bleed trips the daily-loss kill. |
| B1a.3 | Lift Vega rigor (look-ahead audit + walk-forward + 2× stress + portfolio rigor) into `alpha-core` | [CC] | ✅ (#34) rigor core already lifted (B0.9d, byte-identical); B1a.3 **finalized** it — threaded perp funding through `run_portfolio_backtest` (portfolio rigor now correct on perps; closed the TODO) + `rigor.py` to 100% + equity-skip test. Vega rigor suites green in `alpha_core`. |
| B1a.4 | Extend rigor: CPCV + embargo + PBO | [CC] | ✅ (#35) `backtest/overfitting.py` (pure-stdlib): CPCV+embargo splits + PBO via CSCV; `config/rigor.yaml`. PBO flags a reversing-overfit control (~1.0) vs a genuine edge (0.00); noise is no-skill (~0.5). |
| B1a.5 | Deflated Sharpe + **keyed** persistent trial ledger (R4): `research_ledger` keyed by (market, family, window); atomic increments | [CC] | ✅ (#36) `backtest/dsr.py` (PSR/DSR, Bailey–LdP, stdlib `NormalDist`) + the keyed SQLite ledger (`market\|family\|window`). DSR penalty bit-for-bit invariant to an unrelated cell. *(The bare-counter `trial_ledger.py` was **superseded by `research/proposal_ledger.py`** at B1b.3b #56 — one idempotent store; the cell-invariance demonstration migrated with it.)* |
| B1a.6 | Roll-forward locked holdout (R5) in a **separate no-ACL DuckDB** store (R6); record `holdout_window_version` | [CC] | ✅ (#38) `data/holdout.py`: holdout bars routed to a physically separate gate-only store at a **disjoint root** (the cold store's Parquet glob can't reach them); proven holdout-free via *every* cold-store read path (Bar reader + DuckDB view). Re-seal **rebuilds** the rolled-forward window (no stale contamination); `holdout_window_version` recorded. |
| B1a.7 | Reproducibility ledger: record strategy/dataset/engine/cost versions + RNG seed per backtest | [CC] | ✅ (#40) `research/reproducibility.py`: `ReproRecord` captures **all** of `run_backtest`'s inputs (engine/dataset/instruments/risk/cost/strategy+params/seed/venue/cash/stress/funding) → a `fingerprint`; `ReproLedger` (SQLite) persists record+result hash and **enforces** reproducibility (same inputs, different result → raises). `run_and_record` binds the record to the run. Proven: a seeded stochastic strategy re-runs bit-for-bit; any input change flips the fingerprint. |
| B1a.8 | vectorbt pre-screen (coarse walk-forward) upstream of CPCV; CPCV compute budget + parallel pool + carry-forward (R7) | [CC] | ✅ **B1a.8a** (#42) `research/prescreen.py` — vectorbt coarse walk-forward (faithful event-driven signals → vbt sim, `direction='both'` for short alpha) culls losers below a Sharpe floor before CPCV; **B1a.8b** (#43) `research/cpcv_budget.py` — persistent `CpcvQueue` + `drain_within_budget` (budget-aware over any Executor; poison-candidate quarantine) carries the unfinished queue, never truncates. vectorbt in the dev group only. |
| B1a.9 | Population calibration harness (R14): synthetic noise / overfit / planted-signal populations; measure false-promote & false-reject rates | [You]+[CC] | ✅ (#45) `research/calibration.py` — noise/overfit/edge controls through the DSR-on-OOS gate. [You] ratified the Tier-2 bounds (false-promote ≤5%, false-reject ≤25%); measured (mean over 20 seeds) **false-promote 0.0, false-reject 0.056** at a realistic strong edge (Sharpe ~2.4, ~5y OOS). Review caught + fixed a rigged false-reject → gate loosened to `dsr.threshold` 0.92 (safe end). The gate is **conservative by design** — it certifies STRONG edges (Sharpe ~2.4+); weaker edges rejected (documented floor). |
| **1a.GATE** | error-rate thresholds met; holdout/ledger unreachable by agents (TEST-3); DSR cell-invariance; reproducibility | [You]+[CC] | ✅ **[You] ratified 2026-06-26** (`docs/adr/0002`) — all four hold: error rates (B1a.9 — false-promote 0 / false-reject 0.056 at a realistic strong edge) + TEST-3 (B1a.6) + DSR cell-invariance (B1a.5) + reproducibility (B1a.7). **Conservative gate accepted** (`dsr.threshold` 0.92; certifies Sharpe ~2.4+ edges). **Phase 1a COMPLETE → Phase 1b open.** |

---

## Phase 1b — AI discovery (constrained track) · [CC]
Goal: autonomous discovery + rigorous backtesting. No money/keys. Delivers the hero loop on the research box.

| ID | Task | Owner | Done-when |
|---|---|---|---|
| B1b.1 | `strategist` agent (constrained): parameterize vetted templates (seed from Vega's ~7 example strategies) + whitelisted primitives; economic-rationale prompt; **in-sample only, never sees the holdout**; originality check + trial-ledger increment | [CC] | ✅ **B1b.1a** (#48) seed-template substrate (registry + R7 examples) **+ B1b.1b** (#50) the strategist `research/strategist.py` — parameterizes a vetted template within a bounded space, validates by constructing, fingerprint-originality + keyed-ledger increment (B1a.5), **holdout-isolated structurally** (takes no data → TEST-3). The **economic-rationale LLM proposer** is the injectable pod-agent wrapper (Mac-CLI + Lemma), deferred. |
| B1b.2 | `quant-analyst` agent: CPCV/PBO, DSR vs trial count, look-ahead/survivorship, cost sensitivity, regime stability → promote/reject/revise | [CC] | ✅ (#52) `research/quant_analyst.py` — composes the OOS-edge screen + **DSR vs the cell trial count** (the promote-work) + fold-consistency + optional cell-PBO into promote/reject/revise. Done-when MET on the B1a.9 populations (overfit→reject, edge→promote ≥85%, noise never promoted), as a rate over seeds at the calibration point. Validates the deflation inputs (the safety footgun); `deflation_inputs()` is the calibration-faithful helper. *(Carried: cost-sensitivity + look-ahead/survivorship checks wire in with real backtests at B1b.3.)* |
| B1b.3 | Workflow A `discovery-cycle`: data prep → strategist → LOOP{ full-rigor backtest (holdout untouched) → quant-analyst → promote? } → survivors | [CC] | ✅ **B1b.3a** (#54) idempotent `proposal_ledger.py` + **B1b.3b(1)** (#56) it's the sole per-cell count source (`TrialLedger` retired, 1a.GATE cell-invariance migrated) + **B1b.3b(2)** (#58) `research/discovery.py` — `run_discovery_cycle` wires propose → backtest → `deflation_inputs` (cumulative count via `trial_index`) → `assess` → survivors. Done-when MET: promotes planted edges, rejects planted overfits; linkage tested. **B1b.3c** (#60) `research/engine_backtester.py` — the **real** `Backtester`: builds the strategy → runs the lifted engine (`run_backtest`, `now` injected from bar-time) on in-sample bars → per-bar returns; the loop now runs **end-to-end on the actual engine** (a real edge promotes). **B1b.3d** (#62) `research/cold_store_bars.py` — the cold-store `bars_for` adapter: resolves a cell `(market, window)` → its `config/discovery.yaml` series and reads **in-sample-only** bars from the no-ACL cold store; **TEST-3 holdout-denial proven** (structural at the store boundary — the adapter is a pure pass-through). The loop now runs on **real cold-store data, no injected seam left**. *(Carried: cross-run cumulative deflation **variance** — the trial *count* is cumulative via `trial_index`, the variance is per-run today.)* |
| B1b.4 | `nightly-discovery` schedule + `/knowledge` RAG seeds | [CC] | ✅ **B1b.4a** (#64) `research/nightly.py` — the **portable nightly run**: `run_nightly_discovery` sweeps every (cell, template) over `config/discovery.yaml` → strategist → real engine (`ColdStoreBarsFor`/`EngineBacktester`) → quant-analyst → survivors; per-cell capital alignment + poison-quarantine + a data-readiness gate (no trial-count burn on empty cells); `scripts/nightly_discovery.py` entry point. Proven on the real cold store (10 cycles fire end-to-end; 0 promote — correct on thin windows). **B1b.4b** (#66) the research-box **systemd cron** (`deploy/systemd/`) that fires it nightly + a JSON run-record (`NightlyReport.summary`, shaped to `discovery_runs`); **B1b.4c** (#66) the `/knowledge` corpus `docs/knowledge/strategies.md` + the upload/`files search` steps. **Build + DEPLOY complete (CC, operator-authorized 2026-06-27):** timer enabled on the research box (next 02:00 UTC; a manual run fired **30 cycles / 0 quarantine / 0 promote** on the sealed richer data — clean + correct), `/knowledge/strategies.md` uploaded + indexed on the pod and `files search` verified. Done-when MET → only **1b.GATE** remains. |
| **1b.GATE** | the discovery **machine** runs end-to-end on real cold-store data (propose → backtest → full rigor gate → promote/reject), validated on free data | [You]+[CC] | ✅ **operator directed Phase-1 closure 2026-06-27 (`docs/adr/0003`).** The machine is proven end-to-end: promotes genuine planted edges + rejects overfits/noise (B1b.2/B1b.3 tests + B1a.9 calibration), runs a clean **30-cycle** sweep on real sealed free data, and is **deployed** nightly on the research box (B1b.4). Demonstrating a real *deployable* edge needs operator-provided **vendor data** → deferred to **M3.0** (Phase 3). |

---

## Phase 2 — Cockpit + copilot + surface · [CC]
Goal: the visible hero — dashboard + copilot + Telegram. Expand to `B2.x` on arrival.

- **M2.1** Vite + lemma-sdk cockpit shell (`watchChanges` + charts). *Expanded → B2.x:*
  - **B2.1a ✅** (#80) the **shell** — the cockpit Lemma app inside the pod bundle (`pod/apps/cockpit/cockpit.json` + `source/`; canonical bundle shape, so `lemma pods import pod/` builds it), Vite + React 19 + `lemma-sdk`, `AuthGuard` + sidebar nav over all 8 views, terminal design system + designed loading/error/empty states, **Overview = a live SDK connectivity proof** (signed-in user + pod table inventory), path-filtered frontend CI (`.github/workflows/cockpit.yml`). Read + governance-command surface only (TEST-8 / TEST-3). Verified authenticated against the live Vault pod (15 tables); reviewed (no blockers). Plan: `pod/apps/cockpit/DESIGN.md`.
  - **B2.1b ✅** (#82) the **live Overview** — a lightweight-charts equity curve fed from `pnl_snapshots` via `useLiveRecords` (watchChanges) + pipeline count tiles. **M2.1 complete.**
- **M2.2 ✅** (#84) System-health strip — worker/research heartbeats (fresh→stale via a `useNow` local clock), reconcile + data freshness, open (**unresolved**) `risk_events`.
- **M2.3 ✅** (#85) Backtests incl. **rejected** — a sortable/filterable table (Sharpe / DSR / PBO / maxDD / net-P&L / verdict); the overfit reject sits beside the survivors. Introduced the generic `DataTable`.
- **M2.4 ✅** the remaining views: **Strategies + Discovery** (B2.4a #86), **Exchanges + Config** (B2.4b #87 — fed by a committed `config/` snapshot, `scripts/snapshot_cockpit_config.py`, + a CI freshness guard), **Risk + governance controls** (B2.4c #88 — issues `commands` arm/flatten/clear-halt, inline-confirm + issuer-gated, **TEST-8**), **Approvals inbox** (B2.4d #89 — read-only; the functional FORM approval is Phase-3 Workflow B).
- **M2.5 ✅** (#91) `desk` copilot — the **desk pod agent** (`pod/agents/desk`: read-only, **15 grants** over the book tables + `/knowledge` RAG; NO `broker_credentials`, NO holdout (TEST-3 — the review caught + closed a holdout-metrics leak by dropping `holdout_used`/`holdout_metrics` off the pod `backtests` table); never trades, defers go-live to the human FORM — TEST-8) + a cockpit **Desk chat** view (`<AgentThread>`). Telegram surface config staged (`pod/surfaces/telegram`); the bot connect is **[You]**. *("other agents as sub-tools" deferred until strategist/quant-analyst/risk-officer exist as pod agents.)*
- **2.GATE 🔶 pending [You]** — **DEPLOYED + verified (operator-authorized, 2026-06-27):** the cockpit is served at **`https://cockpit.apps.lemma.work`** (READY; renders authenticated; the change-stream **WS is OPEN** — watchChanges is live in production); the TEST-3 holdout-column drop is applied live. **Telegram desk surface LIVE + END-TO-END VERIFIED (2026-06-28, #107):** [You] connected a **dedicated Alpha bot** to the Lemma connector (account `019f0dd2-f652-743d-8939-e78cc093d868`); the surface is rebound to it (`lemma surface upsert TELEGRAM` → ACTIVE/CUSTOM/Ready/agent=desk) and a real DM ("How many strategies are deployed?") got a correct table-grounded reply ("3 deployed, all paper, none live; the pending-approval one → cockpit Approvals FORM"). desk is **pinned to Claude Code / Opus** (#109, `agent_runtime` → Claude Code daemon, opus — was unpinned → cloud minimax/Codex), verified via the daemon log (`claude stream event … provider result`). **To finish 2.GATE:** (a) **daemon-persistence** — pinning to Opus made desk **daemon-dependent** (Lemma has NO daemon→cloud fallback, confirmed in the SDK), so desk only answers while `lemma daemon` is up; for always-on either run the daemon as a service (ideally the box) OR move desk to a **cloud Anthropic runtime profile** (BYO key, no daemon). The **cockpit dashboard is unaffected** (no LLM/daemon). (b) **[You] ratification**; (c) **DM-ratification DEFERRED** (desk is read-only). *(Demo data: `pod/seed/seed_cockpit_demo.sh` #83.)*

## Phase 3 — Paper + approval gate + worker (paper) + deadman · [CC]/[You]
Goal: the full paper pipeline with safety. Proves **TEST-2, TEST-4, TEST-5, TEST-6, TEST-7, TEST-8**.

- **M3.0 — Real-data on-ramp** 🔶 **ingest CODE ✅ (#96/#127–#130)** — the free-data backtest on-ramp is built: `data/ingest/binance.py` + `scripts/ingest_binance_archive.py` (Binance free archive, verified on 86,400 1s bars, #96) → widened to **8 USDT perps × {5m,1h,1d}** (#127) + an **8-symbol 1-second** universe driver (`scripts/ingest_binance_1s_universe.py`, #130) + the **Kite 1-minute** Indian leg (`data/ingest/kite.py` transform #128 + `scripts/ingest_kite.py` auth-glue / `KITE_ACCESS_TOKEN_AT`-staleness → 2FA reauth / `historical_data` ingest #129) + their discovery cells (the nightly's `cell_ready` guard **skips** a cell whose data isn't ingested yet, so cells can land before data — never breaks a running sweep). **Remaining (operational + [You]):** the **box ingest** (the 1s-archive batch + the Kite pull — Kite needs the operator's fresh daily token via the 2FA flow `ingest_kite.py` drives) → `seal` → the first **real discovery sweep** on the wider universe → a deployable survivor (feeds Workflow B, M3.7). Tardis/Velo + Dhan/Upstox optional later.
- **M3.1** ✅ — worker on the AWS box in **PAPER** (#97 adapter factory + live gate, #100 run loop, #101 systemd, #103 Delta India `testnet_url`). **Now running on Delta India perp testnet** (`BTC/USD:USD`); the factory builds Binance/Delta-testnet ccxt + **refuses live without the gate**. (Dhan/Upstox equities deferred — Indian leg.)
- **M3.2** ✅ — risk gate + latching kill-switch + `restore()` + reconcile on the worker (Vega-lifted; #100 added `OMS.reconcile_locked` (atomic) + a kill-switch **halt-generation** so a re-trip always flattens). **Reconcile-truth + kill-flatten demonstrated live on Delta perps** (TEST-4/7).
- **M3.3** ✅ (#94 deadman + #101 `alpha-deadman.service`) — the independent deadman flattens via broker REST on a stale heartbeat. **Demonstrated LIVE on Delta perps (3.GATE):** a real `BTC/USD:USD` position + a stale heartbeat → the deadman flattened it via REST → broker flat. **Remaining:** exchange-side cancel-on-disconnect / reduce-only (a Delta venue capability).
- **M3.4** 🔶 — the **local heartbeat file** (deadman liveness, per-tick) ✅; the **pod** `worker_status`/`research_status` heartbeats + stale-alert DATASTORE schedules = the **pod-sync** increment (not built).
- **M3.5** ✅ — derived-P&L fold over immutable fills (OMS `rebuild_state` + `apply_fill`, Vega-lifted; never mutated from trigger payloads, TEST-2/R11).
- **M3.6** ✅ (#95 portable + #116 pod-sync) — `CommandWatcher` + `emergency_flatten` trip path (TEST-8) + the pod-backed `PodCommandSource` (`records.list` poll + ~1s fallback) wired in `build_worker`; verified live (a `pause`/`start` issued into the pod `commands` table → polled → executed → acked).
- **M3.7** ✅ (#105 portable core + #118 pod Workflow B) — `research/paper_eval.py` `evaluate_paper_run` + `research/holdout_gate.py` `HoldoutGate`/`HoldoutBarsFor` (the one-shot risk-officer gate; the **single legitimate holdout read**, TEST-3; passes only on PROMOTE) **+ the pod approval pipeline (#118):** the **off-pod** risk-officer bridge (`scripts/risk_officer_review.py`) runs both gates and writes an **agent-EXCLUDED** `approval_requests` row (only a scalar verdict crosses; the holdout read stays off-pod, TEST-3) → the `deploy_approval` workflow (a **human FORM** assigned to [You]) → `finalize_deployment` issues the worker `start` command (TEST-8: the pod issues; the worker executes). Verified end-to-end live on the Vault pod (DATASTORE trigger 1:1; FORM waits; approve emits one command, reject none, idempotent on retry).
- **3.GATE** ✅ **RATIFIED by [You] 2026-06-28 (ADR 0004).** The live safety machinery + the human approval pipeline are demonstrated end-to-end. (a) The **safety drill** (CC) — one run on **Delta India perp testnet** (`BTC/USD:USD`) showed all 5: derived P&L (TEST-2), idempotent COIDs (TEST-6), kill-switch flatten (TEST-4), broker-truth reconcile (TEST-7), and the headline — **a real perp position + a stale worker heartbeat → the INDEPENDENT deadman flattened it via REST → broker flat (TEST-5)**. (b) The **full approval pipeline** (#118, live) — a TEST strategy ran strategy→paper→risk-officer/holdout→**human FORM**, and on [You]'s approval `finalize_deployment` issued a `start` command → the worker polled (~1s), executed, and **acked** it (TEST-3 holdout-off-pod + TEST-8 pod-issues-only held). **Deferred to M3.0 (ADR 0004, same as 1b.GATE/ADR 0003):** paper P&L ≈ backtest on a REAL edge (needs vendor data). Flatten-from-Telegram deferred.

## Phase 4 — First real money: Delta live (staged) + SEBI long-poles · [You]+[CC]
Goal: earn the first real money on the lowest-compliance venue, while starting the slow SEBI work.

- **M4.1** Worker → LIVE on **Delta** at tiny size; staged capital 1%→5%→25%→full gated on live-vs-paper tracking error + zero `risk_events`.
- **M4.2** Exchange-side cancel-on-disconnect + reduce-only stops live (R1); CA sign-off on crypto-derivative income.
- **M4.3** *(parallel, long-lead)* SEBI algo registration + static/elastic-IP feasibility with the broker (R2).
- **M4.4** Daily Kite token relay (Mac 2FA → RLS row), fail-closed + on-demand Telegram reauth (R12).
- **M4.5** Kite **live-feed** paper (live ticker WS → strategy → simulated fills), not replay (R9).
- **4.GATE** Delta live runs at small size with clean reconcile across N sessions; equity prerequisites confirmed.

## Phase 5 — Indian equities/index live + breadth · [You]+[CC]
Goal: the real edge — liquid equities/index/options, minutes-to-daily; multi-venue concurrency.

- **M5.1** Kite/Dhan/Upstox live (equities/index + currency derivatives), staged capital.
- **M5.2** Multi-venue + multi-strategy orchestration (clean-sheet — Vega is single-adapter/process): per-venue adapter processes behind a registry + cross-venue position/risk aggregation.
- **M5.3** Elastic-IP failover + RTO/RPO runbook (R2); standby `restore()`s kill-switch + positions.
- **M5.4** ClickHouse warm store **only if** tick volume/query latency now demands it.
- **M5.5** **Options layer (deferred here from M3.0, [You] 2026-06-28).** The whole option capability, which does not exist today (only the `INDEX_OPTION` asset-class enum): the option **instrument model** (strike/expiry/type), an **option-aware strategy contract**, the **backtest + cost model** (option pricing/Greeks as needed), **Indian F&O options data** ingest (Kite — NIFTY/BANKNIFTY chains) + discovery → staged **Kite F&O options live**. This phase's goal already names options, so it is their home; they were explicitly scoped OUT of the M3.0 data on-ramp to keep the first real-edge hunt moving. Crypto options (Binance EOptions) are an additive extension of the same layer. **🔶 RESEARCH ON-RAMP STARTED ([You] chose options as the next research input, 2026-07-02): the DATA layer landed (#150 — NSE F&O bhavcopy, better than the Kite plan: FREE + a decade deep) and the decade is INGESTED (8.9M contract-days, 2016→2026-06; see the M5.5 bullet in 🟡 carried). Only research runs on it; every DEPLOYMENT gate here (SEBI, staged live) is unchanged.**
- **5.GATE** equity live ≈ paper on a live-feed paper window; staged capital advances.

## Phase 6 — Free-form code-gen + scale · [CC]/[You]
Goal: additive, last — the riskiest discovery track, hard-gated on isolation.

- **M6.1** Sandboxed free-form strategist (R3/D2): true OS-level isolation — gVisor/Firecracker + seccomp + cgroups + no network namespace + pure-function IPC; strategy can't reach holdout/ledger/network.
- **M6.2** Portfolio optimization across approved strategies.
- **M6.3** RL / agentic discovery; reconsider the engine only if scale/latency demands change.

---

## ⏳ Pending tracker — the single live worklist (pending items only)

> Every open item appears here exactly once. When it lands, mark ✅ in its phase table above and remove
> it here. Update via `/session-wrap` at the end of every session.

### 🟢 Unblocked — ready to start now
- **Phase 3 portable core + paper worker BUILT + DEPLOYED + RUNNING + self-tested (M3.1–M3.3/M3.5/M3.6 ✅, #94–#103).** The worker (`worker/loop.py` + adapter factory + deadman + commands + BarBuilder + the Vega-lifted risk/reconcile/OMS) runs on the AWS box as **`alpha-worker` + `alpha-deadman` systemd units** (active, 0 restarts) against **Delta India perp testnet** (`BTC/USD:USD`). **The 3.GATE safety drill passed live** (TEST-2/4/5/6/7 in one run — incl. the deadman flattening a real perp position via REST on a stale heartbeat). `idle` no-trade soak (waiting for the first FORM). Monitor: `journalctl -u alpha-worker -f`.
  - **Worker soak HARDENED + verified STABLE 2026-06-28 (#111–#113).** Three fixes: (#111) **position-aware feed-stale** — soft/auto-recover when FLAT, latching kill only when HOLDING a position (the old guard latched the flat idle soak on a >60s stall and stayed halted); (#112) an **offline gated re-arm CLI** (`python -m worker.rearm_main paper`) — re-arms a latched worker only on a CLEAN reconcile (broker=truth) + durably persists the clear (`OMS.clear_persisted_halt`), for when the pod command channel isn't up; (#113, the headline) a **non-streaming venue was mis-wired to the inline `drain_events`**, which `async for`s the *infinite* Delta order-events poll → the market loop wedged on the **first closed bar** (heartbeat froze ~30s in → deadman tripped) — fixed by `drain_inline=False` so the worker always uses the long-lived `consume_events` task. **Re-soak: heartbeat tracks real-time, 0 feed-stale, 0 latch, reconcile CLEAN.** *(Noted minor follow-up: the deadman fires once on restart against the stale leftover heartbeat then recovers in ~3s — a deploy-time heartbeat clear or a deadman startup-grace would silence it; touches the TEST-5 path so deferred.)*
- **M3.7 Workflow B ✅ COMPLETE (#105 portable + #118 pod).** The full per-strategy human approval gate, verified end-to-end live on the Vault pod. **Off-pod** (`scripts/risk_officer_review.py`): runs `evaluate_paper_run` + the one-shot `HoldoutGate` (the holdout read stays off-pod, TEST-3) and, only when both pass, writes an **agent-EXCLUDED** `approval_requests` row (only a scalar verdict crosses — `RiskOfficerReview.request_fields`, never a return series). **On-pod** (`pod/workflows/deploy_approval`): the `approval_requests` INSERT (DATASTORE schedule) → a **human FORM** assigned to [You] → `finalize_deployment` issues the worker `start` command on approve (TEST-8: the pod issues; the worker executes), records the rejection on reject, and is **idempotent** (a retry never re-emits). `pod/tables/approval_requests` is granted to **no agent** (the dedicated agent-excluded table the `backtests` comment prescribed). *Cockpit Approvals view → `approval_requests` ✅ DONE (#123, deployed): the human sees the paper + holdout verdict in-app (read under their own identity, not an agent — TEST-3).*
- **POD-SYNC DONE + VERIFIED LIVE END-TO-END (M3.4/M3.6 — #115 write, #116 read).** The worker writes a **`worker_status`** heartbeat (armed = not halted; detail = run_state/strategy/venue/P&L **as str** (B5)/positions_hash — confirmed updating in the Vault pod in real time) and reads the pod-backed **`PodCommandSource`** (the cockpit's governance commands → the existing tested `CommandWatcher` in `build_worker`). **Verified e2e on the live pod:** a real `pause`/`start` command issued into the pod `commands` table was polled (1s) → executed (run_state→paused→running) → acked (status→done). `lemma-sdk` v0.5.3 is a **worker** dep (never `alpha-core`); strictly **best-effort (TEST-8** — every pod call swallowed/`to_thread`/10s-timeout; a pod outage never touches trading or safety; `ack` propagates so an un-acked command never fires twice). **Deploy:** rsync `alpha-core`/`worker`/`config` → `~/.local/bin/uv sync` (installs lemma-sdk on the box) → restart `alpha-worker`; **token** self-fetched via `lemma auth print-token` on the Mac → piped over ssh stdin to the box's gitignored `.env` as `LEMMA_TOKEN` (value never printed). **Polish:** (a) **token hot-reload ✅ (#121)** — the worker re-reads `LEMMA_TOKEN` from `.env` every 60s and swaps the pod client with **no restart** (`Worker._pod_token_refresh_loop`), so any freshly-staged token is picked up live (delivery-agnostic). *(The Mac launchd relay that fed it was REMOVED — [You] 2026-06-28: a token-minting job on the laptop is the wrong place + fragile; the worker hot-reload stays, and the minting source awaits a **Lemma long-lived/service token** or **box self-mint** — `deploy/relay/README.md`.)* (b) **per-event sync — `risk_events` ✅ DONE (#124):** a kill-switch trip is mirrored to the pod `risk_events` log (off the money path, TEST-8; the cockpit Risk view renders it). The **trade-coupled tables (fills/orders/positions/pnl_snapshots)** require a `deployment_id`/`order_id` FK the idle soak has no source for → they land with **M3.0** (a real trading run to exercise them).

### 🟡 Carried / deferred (not blocking Phase 2)
- **M3.0 — real-data on-ramp (operator-directed 2026-06-28; [CC] builds, no vendor needed).** The ingest **CODE is LANDED** (#127–#130): the crypto universe widened to **8 USDT perps × {5m,1h,1d}** (#127) + an **8-symbol 1-second** leg (`scripts/ingest_binance_1s_universe.py` over the `data.binance.vision` aggTrades archive, #130) + the **Kite 1-minute** Indian leg (the pure transform #128 + `scripts/ingest_kite.py`: read `KITE_ACCESS_TOKEN_AT`, and **when stale drive the daily-2FA reauth** — the human logs in, the script does the `generate_session` token exchange + persists to the gitignored `.env`, value never printed — then resolve instrument tokens + `historical_data(.., "minute")` → cold store; #129) + their discovery cells (24 crypto + 8×1s + 4 NSE-1m). NB the nightly's `cell_ready` guard **skips** any cell whose data isn't ingested yet (trial count preserved), so these cells **do not break the running sweep** and activate automatically once their data lands. **EDGE-HUNT RAN — CONCLUSION: free-data price-only TA is tapped out (2026-06-29/30).** Box ingest DONE + sealed (**40 cells**: 8 crypto USDT-perps × {5m,1h,1d,1m} + 4 NSE × {1d,1m}); the **1-second leg is RULED OUT** — the `t4g.small` box OOM-kills the 1s ingest and *hangs* the instance on a 1s sweep (needs a bigger research box, documented #133). **Three sweeps run** — 28-cell, 1-minute, and a **9-template** sweep after adding 4 **richer** families (`macd`, `donchian_atr`, `bollinger_squeeze`, `trend_pullback` — #136, atop #134 the `--survivor` wiring + #135 the 1-minute universe). Across all three: **7 in-sample survivors, 0 survive the holdout** (`band_bps=55` + 6 from the 9-template run — incl. the new `trend_pullback` and 5 overfit `opening_range_breakout` variants on one cell — all REJECT, holdout OOS-Sharpe ≤ 0). The discovery→rigor→holdout machine is **validated** (it finds candidates and correctly rejects every overfit, simple *and* rich). **So the bottleneck is the DATA / market-efficiency, NOT the strategy space** — price-only TA on liquid free markets (crypto + NSE, 1m–daily) holds no generalizable edge; more TA variants won't change it. **⏳ OPEN STRATEGIC DECISION [You] — pick a DIFFERENT INPUT:** (1) **alternative/vendor data** (order-book depth, funding/OI, options-flow, fundamentals — Tardis/Velo, paid); (2) a **different strategy *class*** (cross-sectional rank across instruments, statistical-arb/pairs/cointegration, ML on engineered features); (3) **less-efficient markets** (small-caps, new listings, the **options layer M5.5**). The **`--survivor` → Workflow B** path is BUILT + proven (#134) and waits for a real survivor. **OUT of scope here (by [You]):** options (its own layer → **M5.5**, Phase 5) and 1-second *Indian* historical (Kite 1-min for backtest now; 1s comes from the live tick WS at execution, M4.5). Tardis/Velo (crypto order-book/derivs) + Dhan/Upstox stay optional later enhancements.
- **M3.0 follow-on — the [You] STRATEGIC pick was TAKEN (2026-06-30): cross-sectional rank + funding carry, both free-data, both with NO holdout-surviving edge.** Built + merged this session: the **42-perp daily panel** (#138), the **cross-sectional panel backtester + momentum/reversal templates** (#139), the **funding-rate data layer** (#141), and the **funding-carry strategy + backtester** (#142); the **panel holdout gate + the decisive read** (#140 — **MERGED 2026-07-01**, squash, per-PR [You] authorization 2026-07-02-plan; verdict recorded). **Findings:** **cross-sectional MOMENTUM** was robustly OOS-positive in-sample (50/50 candidates, 2 DSR promotes 0.93–0.95) but **REJECTED at the holdout** — both survivors *sign-reversed* on the never-seen 2025-11-09→2026-06-21 window (the same regime-luck pattern as the single-instrument survivors); **cross-sectional reversal** found 0 survivors; **funding CARRY** was positive-but-marginal in-sample (42/50 OOS-positive, best OOS Sharpe +0.145 but DSR 0.810 < 0.92 → 0 survivors — a perp-only carry book still bears price risk that dilutes the funding signal); **open interest is OUT** (Binance free `openInterestHist` caps at ~30 days). **So the FREE-DATA edge hunt — price (single-instrument + cross-sectional momentum/reversal) AND funding carry — is now thoroughly explored with NO deployable edge.** The discovery→rigor→holdout machine + ALL infra (panel universe, rigor, `HoldoutPanelBarsFor`/`build_panel_backtesters`/`panel_holdout_review.py`, funding data+carry) are BUILT + VALIDATED. **⏳ OPEN [You] STRATEGIC FORK (a bigger INPUT, not more free-data tweaks):** (1) **delta-neutral BASIS trade** (perp vs spot — isolates the carry from the price risk that diluted it; spot data is FREE, a 2-leg build; the most targeted cheap next step); (2) **paid data** (Tardis order-book / normalized derivs — real cost); (3) **less-efficient markets** (small-caps / new listings / options M5.5); (4) **pause** — the engine is done, resume when richer data/markets are chosen.
- **M3.0 follow-on part 2 — [You] CHOSE "fix the science + basis" (2026-07-02); the SCIENCE-FIX CODE is BUILT (this session's PR — review it, then run the box re-run).** The design+code review found the M3.0 "no free-data edge" verdict **overdrawn** — 4 instrument problems biased research toward false-negative, all now fixed on one branch (PR #140 was squash-merged first, per-PR [You] authorization): **(1) costs ~2.5× overcharged** — `costs.yaml` charged the 10 bps *spot* taker + spot TDS + 8 bps slippage on PERP trades; split into `crypto_perp` (5 bps taker + 3 bps slippage, no TDS; `execution/costs.py` maps `AssetClass.CRYPTO` here) vs `crypto_spot` (10 bps + 1% TDS); **(2) the price panel ignored funding** — one shared `simulate_panel` fold now adds the funding transfer a dollar-neutral perp book pays/earns (`PanelBacktester(panel_funding_for=…)`, wired via `build_panel_backtesters(funding_store=…)` / `ALPHA_FUNDING_ROOT`); **(3) the holdout gate discarded half its window** — new `rigor.yaml holdout_eval.oos_fraction: 1.0` (the WHOLE holdout is OOS to a frozen candidate); R14 harness re-run at holdout-shaped inputs (n=224, 40 trials, drift 0.15, 20 seeds): false-promote **0→0**, false-reject **0.9825→0.8750** (pinned in `test_calibration.py`) — **NB this changes the gate's operating point; flagged for [You]'s async review**; **(4) the panel wasted >half the free span** — `align_closes` is now a **union timeline with point-in-time membership** (a symbol enters once its trailing window is real; a held symbol earning only across real closes), daily ingest extended to **2019-09-01** (`ingest_cold_store._1D_START`, funding to match, `_END`→2026-06-30), and `seal_cold_store` **pins the holdout boundary monotonic** (floored at the prior `_windows.json` start — the 2025-11-09 boundary can never move backward; a never-sealed series inherits its interval's floor). Plus **richer books for the re-run**: `VolScaledMomentum/Reversal` (inverse-vol legs) + `BetaNeutralMomentum` (BTC-beta hedge — the exact failure mode of the M3.0 momentum survivors) registered in `PANEL_TEMPLATES`, and `scripts/panel_holdout_review.py` is now the **disciplined multi-family driver** (one ledger, one holdout read per survivor, carry via `build_funding_panel_backtesters`). **✅ THE RE-RUN RAN (2026-07-02, on the Mac — daily-only, the box untouched):** ingest (majors to 2019-09; **250,935 funding rows / 42 perps**; 74 cold-store series) → seal — **the monotonic floor HELD: all 42 daily holdout starts pinned at 2025-11-09, 0 violations** (BTC ratcheted its own prior; the 41 never-sealed members inherited it); research span now ~2,130 daily bars (≈5.8y) for the majors; holdout = 2025-11-09→2026-06-30 (~233d, judged full-window) → `panel_holdout_review.py`, all **6 families × 50 seeded candidates** (seed 10), one ledger. **VERDICT: 0/8 holdout PASS — the free-data directional conclusion now stands PROPERLY POWERED** (5.8y span, honest 8 bps/side perp costs, funding-aware folds, full-window holdout). Per family: **carry 0 in-sample survivors** (even at honest costs — price risk, not fees, drowns an unhedged carry book); **reversal + vol-scaled reversal 0**; **momentum 3 + vol-scaled momentum 4 + beta-neutral momentum 1 = 8 in-sample DSR promotes** (in-sample OOS per-bar +0.077…+0.093 ≈ ann 1.5–1.8) → **all 8 REJECT on the never-seen window**: 7 sign-flipped negative (−0.009…−0.101) *including the BTC-beta-hedged book* (so the reversal is the xsec-momentum factor itself, not beta exposure), 1 positive (+0.019) but DSR 0.40 ≪ 0.92. **8 holdout reads recorded on the 2025-11-09→2026-06-30 window** (momentum ×3, momentum_volscaled ×4, momentum_betaneutral ×1) — re-seal before any future family reads it (the floor makes that safe). **✅ THE BASIS TRACK BUILT + RUN (2026-07-02, #146 — build → CI green → review sub-agent SHIP/no-blockers → 3 fixes applied → squash-merged):** `Venue.BINANCE_SPOT` + spot daily ingest (api/v3, 40 spot twins — the 1000x-prefixed two excluded as scale-mismatched) + `BasisCarry` (top-k POSITIVE trailing-funding selection, flat when none qualify) + `BasisPanelBacktester`/`simulate_basis` (two-leg fold on one union timeline; per-leg point-in-time membership; carry at `funding[i+1]`; combined 26 bps `crypto_perp`+`crypto_spot` turnover cost; TDS excluded as a creditable withholding — the [You]/CA deploy call) + `build_basis_backtesters` (TEST-3: one boundary object per side) + **`seal_cold_store(seed_floors=…)`/`_FLOOR_TWINS`** (the brand-new spot venue's first boundary pinned at the perps' — **verified live: floor-seed printed and all 40 spot series sealed at exactly 2025-11-09, 0 violations**). Then the seeded 50-candidate run (`--templates basis_carry`): **VERDICT — 0/50 in-sample survivors; the holdout was NEVER read (stays sealed for this family).** **The diagnosis (research-plane, seed-reproduced) is the real finding:** the basis book **works** — slow-rebalance candidates earned **+10–11.7%/yr at 2.5–3% vol (full-series in-sample Sharpe ≈3–4.5) net of honest two-leg costs, ~99.5% invested** — but the profits are **era-concentrated in 2019→2022** (the funding mania): on the gate's OOS slice (≈2022-09→2025-11) the median candidate is ann ≈ **−0.9** and the best only ann ≈ **+1.4** (post-2022 funding on majors ≈ breakeven net of re-ranking churn), while fast books (hold 1–3d) lose −14…−20%/yr — turnover is the killer variable. The huge cross-trial dispersion (variance 2.4e-02, ~100× the price families') is this REAL param spread, which inflates the DSR benchmark to ann ≈6.7 → nothing certifiable. **✅ THE LOW-CHURN VARIANT RAN ([You] chose (a), 2026-07-02, #148 — hysteresis `BasisCarryHold` + maker scenario; build → CI → review SHIP → 3 fixes (incl. strategist `max_attempts=500` so a tiny space is provably exhausted at any seed) → merged):** the 24-config pre-registered space (lookback 7–14 × entry {5,10,15}%/yr; top_k=8 / weekly checks / exit_fraction 0.5 fixed) saturated cleanly. **In-sample: the churn diagnosis CONFIRMED** — maker (12 bps) **24/24 DSR-promote** (13–15.3%/yr @ ~2% vol; the entry-15% books flat ~46% of the time = the selective-desk profile); taker (26 bps) **15/24 promote** (in-sample OOS per-bar +0.21…+0.40 across mid-2022→2025 — the very era the re-rank book called breakeven). **HOLDOUT: 0/15 PASS — the carry itself was ABSENT in 2025-11→2026-06:** the always-in (entry-5%) books realized **negative** holdout Sharpe (−0.09…−0.23), and the selective (entry-15%) books stayed **FLAT the entire window (+0.0000)** — correct desk behaviour in a no-carry regime, but a flat book gives a Sharpe gate nothing to promote. **15 holdout reads recorded on 2025-11-09→2026-06-30 (basis_carry_hold, taker scenario; the maker sweep was in-sample-only — no read).** **THE FREE-DATA CHAPTER IS NOW CLOSED AT EVERY SEAM with honest instruments:** direction (0/8, every book construction), re-rank carry (0/50 in-sample), low-churn carry (in-sample-strong under both cost scenarios → holdout flat/negative — the carry is REAL but REGIME-CONDITIONAL, and the most-recent regime had none to harvest). **⏳ NEXT — [You] strategic:** (1) **paid data / options M5.5 / pause** (the remaining inputs); (2) optionally a cheap **funding-regime tripwire** (a monitor alerting when the trailing cross-sectional funding spread re-crosses an entry threshold — the moment a basis desk becomes live-relevant again; monitoring only — no holdout, no deployment); (3) the paper-pilot-on-economic-grounds call is now **less attractive** — the current regime shows nothing to harvest (a pilot would sit flat or bleed). Deploy-time gates unchanged: venue, spot-TDS vs dated-future (CA call), and **real venue funding accrual in the worker** (`execution/funding.py` still accrues a flat configured rate — WRONG for a funding-centric deployment; must land before any basis paper/live). *(Ops 2026-07-02 EOD: worktrees PRUNED + all 22 merged LOCAL branches deleted (SHAs in the session log). Still [You] (attempted, blocked): **remote branches** — the classifier hard-blocks CC from remote deletion; run `git push origin --delete m30-xsec-holdout claude/refine-local-plan-pdd0ai b2.5-desk-copilot docs-m30-holdout-research-box feat-promote-survivor feat-richer-strategies` (all six verified MERGED); **box timer** — SSH to `3.6.176.133` times out from this network (security-group allowlist?); on the box: `sudo systemctl disable --now nightly-discovery.timer` (worker/deadman untouched).)*
- **M5.5 research on-ramp — [You] CHOSE OPTIONS (2026-07-02); the DATA LAYER LANDED (#150) + the DECADE INGESTED.** The data question resolved better than the Phase-5 plan assumed: **NSE's F&O bhavcopy archive is FREE in both formats** (probed live — UDiFF 2024-07→today, incl. `UndrlygPric` + lot size; legacy confirmed ≥2016), so options *research* needs no Kite token and no vendor. **#150** (build → CI → review sub-agent fix-then-ship → 4 findings applied → merged): `OptionQuote` (Decimal B5; IST calendar days as UTC-midnight labels; zero-volume rows kept — settlement is the daily mark) + `OptionsStore` (Parquet per (venue, underlying, year); idempotent dedup by contract-day with **Decimal-VALUE keys** — parquet decimal128 scale-normalizes, so string keys would silently defeat dedup; DuckDB `option_quotes` view — quote the SQL-keyword `"right"` in ad-hoc queries) + both-format pure transforms (file-level schema-drift guards; locale-proof month map, not `%b`; fail-loud fractional counts) + `scripts/ingest_nse_fo.py` (era-appropriate format with cross-fallback, 404 = holiday skip, monthly write batches, `ALPHA_OPTIONS_ROOT` → gitignored `data_options/`). **⚠️ THE EXPIRY-DAY SETTLE TRAP (the review's headline — P&L-critical for M5.5b):** on a contract's expiry day NSE publishes the **underlying's final-settlement level** in the settlement column (verified live: 293/293 expiring rows, 0/1475 non-expiring) — a fold must mark expiring rows at **intrinsic/close, never `settle`**, else every expiry day corrupts P&L by the full index level (now documented in both module docstrings + the field comment). **INGESTED (Mac): 8,900,231 contract-days over 2,587 trading days, 2016-01-01→2026-06-30** (NIFTY 5.53M + BANKNIFTY 3.37M; 151 holidays skipped cleanly; 162M across 22 year files) — ~90× the crypto panel's volume, in the least-efficient liquid market this stack reaches. **✅ M5.5b BUILT + RUN (2026-07-02/03, #152 + the #153 era fix):** the chain fold (`OptionsChainBacktester` — one condor at a time, entry at EOD settles via the engine `CostModel` per leg, daily marks at settles, hold-to-expiry; `IronCondorEod` selection off the **parity-implied forward**, min |C−P| strike; a 12-config pre-registered grid {7,28}-DTE × {2,4,6}% shorts × {1,2}% wings; OI≥500 floor) + the **options seal** run (**fresh 2024-05-24→2026-06-30 holdout, ~2.1y — NEVER read**). **⚠️ THE TRAP HAD A SECOND ERA (#153 — found by leg-level attribution of an impossible −19%/yr bleed):** NSE's expiry-day settlement column was **0 through 2020-01-30** (the final premium lives in `close`) and the **underlying's final level only from 2020-02-06** — never the option's premium; the buggy fold handed every old-era put its full strike. Fixed era-proof: `settlement_level` = the shared positive settle when present, else the **median expiry-parity estimate** `S = K + C_close − P_close` (robust to stale ITM closes); both-era end-to-end trap tests assert identical settlement. **No holdout was ever read through the buggy fold** (0 in-sample survivors) — the discipline's third save. **THE CORRECTED VERDICT (seeded, 12 configs/cell): NIFTY 0/12 + BANKNIFTY 0/12 in-sample — ZERO options-holdout reads spent; both holdouts stay virgin.** Diagnosis: NIFTY condors all mildly negative (−0.4…−1.7%/yr @ 1–3.6% vol); **BANKNIFTY weeklies mildly positive (+0.1…+1.4%/yr, best in-sample OOS ann Sharpe 0.58)** — the premium is real but the retail-simplest EOD hold-to-expiry harvest **does not clear the Indian cost stack at DSR significance** (the OOS slice sits entirely post-2020-02, so the era bug never touched the gate's judgment — verified by identical OOS Sharpes pre/post fix). **⏳ NEXT ([You] direction):** the options design space is now cheap to iterate — each NEW pre-registered family (its own tiny grid, own trials, the virgin holdout waiting) costs ~an hour end-to-end: candidates incl. expiry-day-only structures, vol-regime-conditioned entries, delta-hedged premium, ratio/broken-wing shapes, or BANKNIFTY-weekly-focused refinements; alternatively pause options here with a clean powered first read. Deployment gates (SEBI, staged live) unchanged.
- **PLATFORM-COMPLETION PROGRAM — [You] PIVOTED (2026-07-03): defer strategy-finding (and the 2.GATE close-out) — build the app END-TO-END first, then resume the strategy hunt.** Approved order: ~~1 2.GATE~~ (deferred) → **2 live funding accrual ✅ → 3 funding tripwire ✅ → 4 pod trade sync ✅** → 5 Kite live-feed paper (M4.5) → 6 market calendar + instrument master → Dhan/Upstox adapters → 7 live options capability (contracts/margin/Greeks gate) → 8 [You] SEBI long-poles in parallel; **M5.2 multi-venue orchestration HELD until a real strategy exists.** Landed so far (all: build → review sub-agent → fixes → CI → squash-merge): **(2) #155 R13 live venue funding accrual** — `BrokerAdapter.funding_rate(symbol, boundary)` (ccxt `fetch_funding_rate_history`, boundary-matched ±300s), worker accrues once per crossed UTC-anchored boundary on the broker-truth book, config rate demoted to a loudly-labelled fallback, accruals persisted via `upsert_daily_pnl` + hash-chain-audited (`FUNDING_ACCRUED`), restart baselines (never double-accrues) — **the basis paper/live deploy gate is cleared**. **(3) #156 funding-regime tripwire** — pure `assess_funding_regime` (sum-window ÷ config lookback ×365, top-k mean vs bar; partial windows deliberately under-stated), `config/research.yaml funding_tripwire:` block, `scripts/funding_tripwire.py` (per-symbol failure-tolerant; `notifier_from_env`), `deploy/systemd/funding-tripwire.{service,timer}` **staged but NOT enabled on the box** (next box deploy: `systemctl enable --now funding-tripwire.timer`); live smoke 2026-07-02: **quiet — top-8 trailing 5.4%/yr vs the 10% bar** (XMR 12.3% the lone hot name). **(4) #157 pod trade sync (the M3.4 trade-coupled deferral, closed)** — `PodTradeSync`: orders upsert by `client_order_id`, fills append-only idempotent by `dedup_key`, positions by `position_key`, cadenced `pnl_snapshots`; delta-sync each telemetry tick, STRICTLY best-effort (TEST-8), store-sourced orders+fills (R11), and a single-mechanism SELF-HEAL (any row failure → re-baseline from pod truth; lost create-acks and pod-side edits converge instead of wedging — review findings, test-pinned). `pod_sync.deployment_id` (UUID-validated; **runbook: new deployment_id ⇒ fresh state_db**) + `pnl_snapshot_seconds`. **EXERCISED against the live pod** (demo `deployments` row `c76a2257…`, stopped after): paper assembly (live public Binance ticks → the real Worker loop → PaperBroker fills) ran 8 min → **18 orders/18 fills/flat close synced: 18 distinct dedup_keys, Σqty 0.018, position realized −0.50193829872095 digit-exact vs the worker log, 8×60s snapshots with `equity = realized − fees` arithmetic-exact; restart-baseline adopted a prior run's row instead of duplicating**. Two platform facts recorded: **Binance spot testnet's user-data ws does not deliver fills** (run 1: instant venue fill → local order stayed live → reconcile-truth flagged + alerted, exactly as designed) and **spot venues reconcile positions as `[]`** → `BINANCE_SPOT` stays a research/data venue; **Delta testnet's API key is IP-pinned to the box** (Mac runs need Binance or paper). The demo harness (`scripts/demo_trade_sync.py` + `config/demo.yaml`, kept **untracked-local**) is the M4.5 paper-trading assembly shape: live ticks + simulated fills through the identical Worker. *(Ops closed 2026-07-03: the 6 stale remote branches deleted; box `nightly-discovery.timer` disabled ([You]-run one-liner; worker+deadman verified active); the AWS-whitelist question answered — Tailscale (rec) or SSM kills the IP chore; [You] account-side signup pending, then the box closes port 22.)* **✅ ITEMS 5–7 ALSO LANDED (same session, 2026-07-03 — the BUILD SIDE of the program is COMPLETE): (5) #159 M4.5 Kite live-feed paper** — `KiteTickerFeed` (kernel; thread→asyncio bridge over Zerodha's ticker ws, protocol-typed so the kernel never imports kiteconnect; FULL-mode depth; naive-IST→UTC; day-cumulative `volume_traded` shipped as per-tick DELTAS per DATA-1) + `TeeFeed` (each tick updates the PaperBroker's quotes AND the BarBuilder — paper fills price off exactly the stream the strategy saw) + **`execution: venue|paper`** in `build_worker` (paper ⇒ PaperBroker is broker-of-record for OMS *and* reconciler; inline bounded drain; `paper_starting_cash` config) + `config/kite-paper.yaml`. **Gate posture: kite venues are market-data-only by construction and `active_venue` enforces execution=paper for them; ccxt venues keep the exact live gate.** Review (fix-then-ship, both reproduced): **PaperBroker now honors the get_orders CONTRACT** (full session incl. terminal snapshots — an instantly-filled market order racing the periodic reconcile now resolves as a CLEAN missed-fill adoption, not a spurious latched halt) and **`seed_from_store`** re-homes orders/positions at boot (a restart-while-holding no longer bricks the startup gate). **THE LIVE EXERCISE AWAITS [You]: stage the daily token (`scripts/ingest_kite.py`) any market day → `uv run python -m worker kite-paper`.** **(6core) #160 NSE trading calendar + session gates (SCHED-1 is now real)** — `config/calendar.yaml` (2025 full circular-verified; 2026 fixed-date partial, ▲ annual ops refresh; fail-safe asymmetry documented: a missing holiday idles, only verified dates may be listed) + `load_trading_calendar` wired into `schedule_for` + the Worker gates: entries blocked outside the session (Kite pre-open snapshot ticks) and past no_new_entry (NB the gate wraps `process_bar` — strategy entries AND exits defer to the next session; only halt/square-off flatteners place in a blocked window), opt-in `intraday_square_off` (once per session date, risk gate stays armed, defers to a latched halt), and — the review's key catch — **the feed-stale kill is now session-scoped** (a closed session's silence is expected; the CNC overnight default no longer kill-flattens at 15:32 nightly); a failed square-off alerts CRITICAL and never announces false success. + `scripts/refresh_instruments.py` (the cached Kite instrument-master refresh). **(7core) #161 options capability in the engine (ADR 0017)** — `alpha_core/options/`: **Black-76** premium/**implied-vol** (bisection; None outside the no-arb band — stale marks degrade to "no IV", never a fabricated number)/**Greeks** (forward delta, vega per 1.00, theta per year; rho None until a rate curve; reviewer recomputed the math independently — all pinned values correct) + `time_to_expiry` (ACT/365 at the venue settle; NSE default, Delta 12:00 UTC param) + `chain.py` (deterministic selection + `nearest_expiry`); **options margin in the risk gate** (registry-known options: long = premium, short = premium + `options_short_pct`·strike notional — SPAN stand-in, refined live) and **step 9b fail-closed Greeks caps** (caps set + option order ⇒ a computed `BookGreeks` is REQUIRED — keyed off asset class too, so a registryless deployment still fails CLOSED). Review blocker fixed + pinned: **a flip THROUGH zero is an entry, not a reduction** (SELL 149 against +75 now runs the full gate; the naked short rejects). **📌 TRACKED FOLLOW-UPS (all pre-conditions, none blocks today's deployments):** (a) pre-live for `intraday_square_off` on a REAL order API: `flatten_all` must net in-flight closes (#160 SF3 — a halt landing mid-square-off could double-flatten); (b) pre-any-equity-paper-vs-backtest comparison: the backtest runner needs the symmetric session gate (#160 SF4, TEST-1); (c) net-vs-gross Greeks caps diverge from Vega ADR 0017's "gross" wording — amend the ADR or add gross caps when options go live; (d) the kite instrument-cache needs an mtime bound before F&O contracts (weekly churn); (e) Dhan/Upstox adapters + the M4.4 token relay + real SPAN await [You] accounts/keys. **⏳ THE PROGRAM'S REMAINING ITEMS ARE ALL [You]-SIDE: the M4.5 live exercise (daily Kite token, any market day), Dhan/Upstox accounts (item 6 tail), SEBI long-poles (item 8), Tailscale/SSM signup (then the box closes port 22), and — whenever chosen — re-opening the strategy hunt (the options holdout stays VIRGIN).**
- **STRATEGY HUNT RE-OPENED — [You] COMMANDED AN INTRADAY MANDATE (2026-07-04): "find sustainable intraday strategies — dig into HFTs, banks, traders, their research."** The internet/literature survey is DONE and recorded in **`docs/research/intraday-edge-survey-2026-07.md`** (evidence + counter-evidence + India-2026 reg/cost map + data map; **no pre-registrations, no holdout reads**). **The verdict on "what was stopping us": input class, not method** — the closed free-data chapter mined generic price signals, while the documented practitioner edges are STRUCTURAL (time-of-day flows, dealer gamma-hedging pressure JFE-2021, expiry mechanics, cross-venue information diffusion ~57s BTC→alt, crypto hour-of-day seasonality) — none of which we ever tested, and most of which need data we already have or can get free. **Ranked pre-registerable families:** **F2** crypto hour-of-day/day-of-week seasonality (free, existing panel machinery, FIRST) → **F1** NIFTY intraday momentum (Zarattini noise-area + Gao first-half-hour + **NGE conditioning computed from our own 8.9M-contract-day EOD chains**; free Kite historical minute ingest — now included in base Kite Connect; requires the tracked SF4 session-aware fold, TEST-1) → **F3** BTC→alt lead-lag at 1s–1m (free Binance aggTrades → 1s bars; costs-first design) → **F4** liquidation-reversion (start the free forceOrder recorder early) → **F5** NIFTY-Tue/SENSEX-Thu expiry-day option structures (**[You]-gated: ~₹15–35k options minute-data pack**, quotes ~₹4.2k+GST/index-yr) → F6 delta-hedged intraday vol (later). **Key 2026 facts pinned:** Budget-2026 STT hikes (futures 0.05% sell, options 0.15% premium sell, Apr 2026 — `costs.yaml` update due when F1/F5 build); weeklies = NIFTY Tue + SENSEX Thu only; NIFTY lot 75; SEBI FY24-25: 91% of individuals lost ₹1.06L cr (the flow that funds the sell side). **⏳ [You] ASKS: (a) approve/defer the ₹15–35k F5 data pack; (b) a Kite daily token on a market day for the F1 minute-history ingest. F2/F3/F4 are unblocked and free.**
- **✅ F2 BUILT + RAN TO A CLEAN VERDICT (2026-07-04; [You]: "crypto now, nse and kite later"). Build (#164: build → review sub-agent FIX-THEN-SHIP → fixes → merged; +#165 print fix):** `SeasonalHourLong`/`SeasonalSundayTrend` in their own `SEASONAL_TEMPLATES` registry (nightly universe untouched) + `scripts/seasonality_review.py` (seeded exhaustive sweep, one-read-per-survivor) + registry `_BUILDERS` entries; **the review blocker — `EngineBacktester` couldn't resolve a non-default template registry — was smoke-caught pre-review, fixed via an injectable `templates:` mapping (threaded through `engine_backtester_for`/`build_survivor_backtesters`), and independently confirmed-by-execution by the reviewer** (its 2,000-trial randomized gap battery found no timing/state defects in the strategies themselves). **Data:** 1h series extended to listing→2026-06 (50–60k bars × 8 perps) + re-sealed — **the monotonic floor HELD: all 8 hourly holdout boundaries stayed pinned at 2026-04-16** (fraction re-seal would have moved them a year backward). **VERDICT (seed 10; all 36/36 pre-registered configs exhausted; honest 8bps/side taker): 0/36 in-sample survivors → ZERO holdout reads — the 1h holdout stays SEALED for this family.** Detail: BTC hour-window **9/9 REJECT, every OOS Sharpe negative** (the 2015-22 late-UTC anomaly does NOT clear taker costs on the 2022-11→2026-04 OOS era — the survey's ~58%/yr-hurdle cost math, confirmed empirically); BTC Sunday-trend 6/9 OOS-positive but best DSR 0.178 ≪ 0.92 (direction matches Concretum; ~1 trade/week has nowhere near certifiable power); ETH hour best DSR 0.332, ETH Sunday best 0.451. Fold-semantics note (TEST-1/4): candidates run the FULL live risk gate — BTC {21:00,2h} latched the daily-loss kill during the 2025-04 tariff-crash window and its track honestly ends there. **(*2026-07-06 asterisk, #180: that kill was the CUMULATIVE-counter defect — `_day_realized` never reset on date rollover — not a true single-day 2% breach (impossible at qty 1 BTC vs a $1M base); the 0/36 verdict is direction-safe — un-truncating a cost-bleeding track only adds losing bars — and zero holdout reads were spent.)** **Optional follow-up (a NEW pre-registration if taken): a maker-scenario hour-window sweep — in-sample-only until a post-only paper fill model exists (a holdout read must match deployable execution). ➡️ NEXT (crypto-first order): F3 BTC→alt lead-lag (free Binance aggTrades → 1s research bars; NB the 1s/tick INGEST must run on the Mac, not the t4g.small box — see the infra bullet below) + the F4 liquidation-recorder alongside.**
- **✅ THE CRYPTO-DERIVATIVES NIGHT RUN (2026-07-04, [You]: "do all 3 up to F4; do not stop until an edge is discovered") — FW CLOSED + F3/F4 BUILT-REVIEWED-MERGED (#167–#171), runs in flight.** **Infra (#167):** month-partitioned `TickStore` (columnar in/out — `BarStore.write_bars` round-trips whole series through pydantic models, the exact t4g.small OOM mechanism) + resumable streaming aggTrades→1s ingest; review MAJOR fixed (DuckDB reads now UTC-pinned — host-tz sessions rendered timestamps IST) + atomic month writes, month-grouped flush, coverage reporting; **a live resume bug was caught mid-run** (a series-end marker masked BTC's entire backfill behind a smoke-test tail → month-coverage resume, reviewer-confirmed). **FW funding-window family (#168 + #169): CLOSED — 0/48 in-sample survivors** (pre-drift + rebound × {30,60}m × {1,3}bps × 6 fresh perps, 3y gapless 1m bars, honest 16bps RT taker): funding-clock effects do NOT clear retail taker costs — most configs negative, best DSR 0.062 ≪ 0.92; **ZERO holdout reads** (moot at 0 survivors — and the interval-level floor had pinned the 6 "fresh" symbols' 1m boundaries at 2026-06-24 anyway: the #144 anti-gaming rule catching its first real attempt; #169 added `--holdout-reads skip` to freeze survivors until a rolled-forward window has power — read-timing chosen on window SIZE, never content). The reviewer verified the look-ahead fence with adversarial tapes (pre-drift conditions ONLY on the previous settled rate) and proved the pipeline promotes a planted edge end-to-end; its required fix (an empty funding store must RAISE, never fake a family-close) is in. **F3 lead-lag (#170, SHIP):** 1s BTC→alt follow fold — 2s charged latency (fixture-pinned: faster-than-latency diffusion earns exactly zero), staleness embargo, non-overlap, exit-minute marks; `tick_seal` (fraction+floor over month partitions; reviewer confirmed interval-1 boundaries are purely fraction-based → a REAL ~5-month holdout) + manifest floor carry-forward for series absent from raw (TEST-3-adjacent; **bar-store seal parity is a tracked follow-up**). Reviewer print-time-verified the latency honesty and brute-force-matched `rolling_sigma` at 1e-9. **F4 liquidation (#171):** the archive truth — Binance NEVER archived um liquidations and the cm series STOPS 2024-10-14 ⇒ no archive-era holdout can exist ⇒ the fold is EXPLORATORY-ONLY (structurally gateless — no holdout twin) and the gate-eligible family will be judged on recorder-era um data; cm events ingested (BTC 106,822 + ETH 56,186, 2023-06-25→2024-10-14); the um forceOrder recorder is live-smoked + staged (`deploy/systemd/liq-recorder.service`, NOT enabled — [You] enables at a box deploy: `sudo systemctl enable --now liq-recorder.service`) — review caught 2 HIGH recorder defects BEFORE months of data accrued: ccxt liquidations have NO `amount` key (size lives in `contracts` — now recorded + raw `info`) and `MemoryMax=128M` would OOM-crash-loop (measured 330MB peak → 512M). **⏳ IN FLIGHT (watchers armed): the 24mo×7-symbol 1s download (BTC done, alts running) → `seal_tick_store` → the F3 seeded sweep (40 trials, holdout reads AUTO for survivors — the first real tick-era gate) → verdict; the F4-era backfill (BTC+ETH 2023-06→2024-06, running in parallel) → `liquidation_explore` (NO GATE — the prior for the recorder-era pre-registration). Verdicts land in the next tracker update.**
- **✅ THE VERDICTS + THE KITE DAY (2026-07-05/06; [You]: "kite is open now — finish all the kite infra changes" → "continue until the F1 sweep verdict lands"). CRYPTO CLOSED: F3 lead-lag 0/40 in-sample, ZERO reads** (5 alts x {15,60}s lead x {2,4}σ x {60,300}s hold, exhaustive; EVERY OOS Sharpe negative — the ~57s BTC→alt diffusion does not survive 16bps RT taker + the honestly-charged 2s latency; the 1s holdout stays sealed). **F4 archive-era exploratory: 16/16 prior-REJECT** (gross edge +3–13bps < costs; recorder-era family waits on `liq-recorder.service` — [You] enables at a box deploy). **THE KITE DAY (all built → review-sub-agent → fix-then-ship → merged): (a)** daily token staged via the 2FA flow + auto box-stage; **(b) M4.5 live exercise RUN** — 33 min of live NSE ticks through `KiteTickerFeed`→TeeFeed→gates (idle soak; **both session gates fired ON the configured boundaries live**: `no_new_entry_cutoff` @15:10:00, `session_closed` @15:30:00; 8+ post-close minutes with NO false stale-kill — the #160 session-scoped fix observed live; pod-sync off this run — no LEMMA_TOKEN staged; full-dress rerun optional); **(c) the F1 data**: 5.28M minute bars ingested 2015-01-09→2026-07-03 (NIFTY 50 index + RELIANCE/TCS/INFY/HDFCBANK) — the deep pull surfaced BOTH Kite old-archive glitch shapes, fixed + test-pinned in **#175** (zero-price rows AND OHLC-incoherent rows skipped per Bar's own validators; all-garbage batches raise); **(d) #174** Budget-2026 STT statute refresh (option STT 0.0625%→0.15% premium-sell — was TWO revisions stale — + the NEW `index_future` cost home ~6bps RT) whose review also caught **#177: GST was silently ₹0 on every Indian-segment order** (YAML-1.1 parses bare `on:` as boolean True; quoted + regression-pinned through the real yaml — the M5.5b condor diagnostics were ~₹4/order optimistic, verdicts direction-safe); **(e) #176 SF4** — the backtest runner now mirrors the Worker's session gate + square-off EXECUTION (cancel→risk-bypassing flatten→verify-flat; review reproduced a resting-limit fill-through and a silently-rejected oversized flatten — both closed + test-pinned; thin-tape rollover flatten added; daily-bar guard; 24x7 short-circuit); **(f) #178** instrument-cache mtime bound (24h, stale-fallback loud); **(g) #180** the daily-loss window now RESETS on the trading-date rollover (`_day_realized` accumulated since-boot — the F2 asterisk above; forward-only roll after the review reproduced a stale-fill backward-roll that wiped the live counter fail-OPEN; TEST-4 latch pinned across the rollover). **F1 BUILT + REGISTERED + RUN (#179; 4 configs exhaustive: lookback {14,90} x conditioning {none, fhh-sign} on nifty50-1m; futures cost overlay `futures_costed_equity_config`; SF4 schedule + MIS square-off on BOTH pair sides; review caught pre-run: the 1M cell base made the risk caps silently forbid entry on 74% of the window → cell runs at 10M; the gap-day inverse-position bug → MIS-required contract, silent reset; 2015 `:01`-stamp check voiding). VERDICT (seed 10, both runs byte-identical): 0/4 in-sample survivors — OOS Sharpe −0.0000…−0.0007, DSR 0.000: the noise-area breakout nets to ~ZERO after the honest futures stack; fhh conditioning changes nothing. ZERO holdout reads — the interval-level floor (#144 anti-gaming, its second catch) pins the fresh NIFTY 1m boundary at the NSE-1m interval floor 2026-06-10 (~17 sessions — unpowered), so the run used `--holdout-reads skip`: read-timing on window SIZE only; the window fattens as data rolls forward.** Forensic note: the four `kill_flatten` lines per sweep are the ENGINE SQUARE-OFF flattening the 2021-02-24 NSE-outage evening session (by design, `halted=False` probe-verified) — `flatten_all`'s log name is misleading (tracked). **📌 NEW TRACKED FOLLOW-UPS:** restart-across-UTC-midnight loses the latch (#180 F3, pre-existing); `kill_flatten` log rename; funding-upsert keying on the rolled window date; `_persist_halt` date keying; terminal-flatten risk-gate rejection (#176 note); F&O stale-cache hard-fail revisit (#178); worker M4.5 full-dress with LEMMA_TOKEN + a trading strategy. **➡️ THE HUNT LEDGER NOW: F2 0/36, FW 0/48, F3 0/40, F1 0/4 — all closed with ZERO holdout reads anywhere (every holdout virgin); F4 = recorder-gated. Remaining registered inputs: the NGE/gamma conditioning family (needs the EOD-chain gamma pipeline — data we already own), a maker-scenario F2 re-registration (#173 plumbing merged), F5 (the ₹15–35k options minute pack — [You]), F1-holdout read when the floor-thinned window fattens (~Q4 2026 for a powered read), and the recorder-era F4.**
- **⚠️ INFRA REQUIREMENT — backtesting needs a SEPARATE, BIGGER research box (do NOT run heavy/tick discovery on the worker box).** The design intends a dedicated **research box** (CLAUDE.md: *"A research box (the AWS machine) runs backtests/discovery with no live keys"*), but in practice the **same box `3.6.176.133` (`t4g.small` = 2 vCPU / 1.8 GB RAM, NO swap)** runs BOTH the live `alpha-worker`/`alpha-deadman` AND discovery — double duty it cannot sustain for heavy backtesting. **Evidence (2026-06-29):** the **1-second** crypto ingest (7-day window) was **OOM-killed** (only 4/8 symbols landed), and a "thin" **1-day** 1s 40-cell sweep **HUNG THE WHOLE INSTANCE** (AWS instance-reachability check failed → required a **console reboot by [You]**; the live worker survived only because it auto-restarts on boot). The 1s data was then removed so the 02:00 nightly timer stays safe. **Requirement for the M3.0 backtesting exercise:** provision a **dedicated research box with materially more RAM + vCPU + swap** (≈ ≥8–16 GB / ≥4 vCPU), **separate** from the live-worker box, so (a) discovery never risks the live worker and (b) 1-second/tick scale is feasible. The `t4g.small` stays fine for the **live paper worker** (idle soak); it is **not** adequate for research. **[You]-gated** (infra/cost). Until then, backtesting on this box is **limited to the lighter daily/5m/1h/1-minute universe — no 1s/tick.**
- **[You]+[CC] gate refinements (optional — both touch the 1a-calibrated gate):** (1) **cell-PBO into `discovery` (#58)** — attempted + reverted: the *existing* hard PBO downgrade over-rejects robust, edge-rich cells (PBO is high for an overfit *selection* **and** for a cell of uniformly-strong edges, so it downgraded all 10 planted edges in the B1b.3 Done-when test). Needs **PBO-as-confirmatory** redesign + re-calibration, not a faithful wire-up — no code shipped. (2) **cumulative DSR deflation variance** — make the variance cumulative across runs (the *count* already is via `trial_index`); changes a calibrated DSR input → needs re-validation. Neither blocks Phase 2.

### 🔒 [You]-gated (later phases)
- **Phase 2 — 2.GATE ([You] ratify + daemon-persistence).** Cockpit + `desk` deployed + verified (#80–#91); cockpit half done. **Telegram desk surface LIVE + END-TO-END VERIFIED (2026-06-28, #107):** [You] connected a **dedicated Alpha bot** (Lemma connector account `019f0dd2-f652-743d-8939-e78cc093d868`); `lemma surface upsert TELEGRAM` rebound the surface to it (ACTIVE/CUSTOM/Ready/desk) and a real Telegram DM got a correct table-grounded reply. desk runs via `lemma daemon` (Mac pid 56820) + the **Codex** harness — NOTE the default pod is now **Vault** (`lemma config set-default-pod`), and the daemon must be restarted to pick up a pod switch. **To finish 2.GATE:** (a) **daemon-persistence** (a service, ideally on the worker box, so desk answers without the laptop); (b) **[You] ratification**; (c) DM-ratification still deferred (desk is read-only). *(The #91 desk PR was **human-merged** per the carve-out — TEST-3.)*
- **Phase 3 ✅ COMPLETE — Delta-testnet keys ([You] provided 2026-06-28), 3.GATE RATIFIED ([You] 2026-06-28, ADR 0004).** `DELTA_TESTNET_*` keyed (Delta India demo, `cdn-ind.testnet.deltaex.org` — staged testnet-only on the box, box IP `3.6.176.133` whitelisted); the 3.GATE safety drill + the full approval pipeline both passed live on `BTC/USD:USD` ([You] approved a test FORM end-to-end). **Only the M3.0 real-edge paper P&L remains, data-gated (🟡 carried).** Live (Phase 4) remains a fresh per-strategy human FORM, never CC.
- The Phase-3/4 money/live gates (paper-approval FORM, Delta live, SEBI long-poles). The **fuller `helpers/config.py`** (universe config + restore `_scan_for_secrets`) rejoins when the live gate lands. **Safety-gate edits** (never-do list / cardinal invariants TEST-1..8 / money-live-secrets / live-gate) → **[You]** merges. *(Agents/pod are **Mac-CLI-only**; the portable core builds anywhere.)*
- **CI required-check note (repo settings, from the B2.1a review):** `cockpit.yml`'s `typecheck-build` is **path-filtered** to `pod/apps/cockpit/**` — correct (Python-only PRs skip it), but if it's ever marked a **required** status check, a Python-only PR won't trigger it and could hang on "waiting for status." If made required, use a path-aware required check (or an always-reporting skip job).

### ▶️ Next action for a cold session
**Phases 0/1a/1b COMPLETE; Phase 2 COMPLETE + DEPLOYED (Telegram desk surface now LIVE on the dedicated Alpha bot, desk pinned to **Opus**; only [You]-ratify + daemon-persistence left). Phase 3 COMPLETE (3.GATE ratified 2026-06-28, ADR 0004; bar the M3.0 data dependency):** the **portable safety core + crypto paper worker are BUILT, DEPLOYED, RUNNING, self-tested**, and the **M3.7 portable core is built** (M3.1–M3.3/M3.5/M3.6 ✅ + M3.7 portable ✅, #94–#109). On the AWS box `3.6.176.133` the `alpha-worker` + `alpha-deadman` **systemd units** run against **Delta India perp testnet** (`BTC/USD:USD`); the **3.GATE safety drill passed live** (TEST-2/4/5/6/7 in one run — incl. the deadman flattening a real perp position via REST on a stale heartbeat). `idle` soak — now **HARDENED + verified STABLE 2026-06-28 (#111–#113)**: position-aware feed-stale (no more stuck-halt), an offline gated re-arm CLI, and the headline `drain_inline` wedge fix (a non-streaming venue mis-wired to the inline drain of the *infinite* order-events poll froze the market loop on the first bar) → re-soak heartbeat tracks real-time, 0 latch/feed-stale, reconcile CLEAN. M3.7 portable = `research/paper_eval.py` + `research/holdout_gate.py` (the one-shot holdout gate, the single legit holdout read, TEST-3). **POD-SYNC DONE + verified LIVE e2e 2026-06-28 (#115 worker_status write, #116 pod `PodCommandSource` read):** the worker↔pod loop works on the Vault pod — the cockpit sees the worker live/armed and a real `pause`/`start` command was executed + acked end-to-end (TEST-8: pod issues, worker executes). **WORKFLOW B ✅ + DRIVEN LIVE (#118):** the per-strategy human approval gate is built + verified end-to-end (off-pod risk-officer/holdout bridge → an **agent-excluded** `approval_requests` row → the DATASTORE-triggered `deploy_approval` FORM → `finalize_deployment` `start` command; TEST-3 + TEST-8 held, idempotent). On [You]'s FORM approval the full pipeline ran **end-to-end live** (→ `finalize_deployment` issued the `start` command → the worker polled ~1s, executed, and **acked** it). **3.GATE RATIFIED 2026-06-28 (ADR 0004).** Phases 0–3 are now closed bar the **M3.0** data dependency; the next phase (Phase 4 — first real money, Delta live staged) is fully [You]-gated.

**Pick up next — UPDATE 2026-07-06: the intraday hunt's FREE inputs are fully swept (F2/FW/F3/F1 all 0-for, zero holdout reads — see "THE VERDICTS + THE KITE DAY" bullet above); next moves are the NGE/gamma family (own data), the maker-F2 re-registration, [You]'s F5 data-pack decision, the F1 holdout read when the floor-thinned window fattens, and the recorder-era F4 (box deploy). The paragraph below is retained as the pre-pivot record.** *(Superseded 2026-07-03 text follows:)* **[You] PIVOTED 2026-07-03: strategy-finding was DEFERRED; the then-active worklist was the PLATFORM-COMPLETION PROGRAM bullet above (items 2–4 ✅ #155/#156/#157 incl. the live-pod trade-sync exercise; NEXT = item 5, M4.5 Kite live-feed paper build). The paragraph below is the state of the strategy-research seam AT the pivot (resume point when [You] re-opens it).** Phase 3 is COMPLETE (3.GATE ratified 2026-06-28, ADR 0004). The [You] fork was RESOLVED 2026-07-02 ("fix the science + basis"): the science-fix code LANDED (#144, [You]-merged, CI-green, post-merge-reviewed) AND the disciplined re-run RAN on the Mac the same day — VERDICT **0/8 holdout PASS, now properly powered** (per-family detail in the *part 2* bullet above). The free-data DIRECTIONAL seam is CLOSED with an honest instrument: momentum is real in-sample (8 DSR promotes at ann ≈1.5–1.8 across plain/vol-scaled/beta-neutral books) but the factor itself reversed in the 2025-11→2026-06 holdout — not beta, not costs, not sample size. THE BASIS TRACK IS ALSO DONE (#146, same day — built → reviewed SHIP → merged → spot ingested → floor-seeded seal verified → 50-candidate run): **0/50 in-sample survivors, holdout NEVER read (sealed for this family)** — but the diagnosis is the real finding: the basis book **works** (slow books +10–11.7%/yr at 2.5–3% vol net of honest two-leg costs, full-series Sharpe ≈3–4.5, ~99.5% invested) yet the profit is **era-concentrated 2019→2022**; on the OOS era (2022-09→2025-11) it is ~breakeven net of re-ranking churn (median ann −0.9, best +1.4; hold-1–3d books lose 14–20%/yr — turnover kills). THE LOW-CHURN VARIANT ([You]'s pick) ALSO RAN (#148, same day — hysteresis `BasisCarryHold`, 24-config pre-registered space, maker scenario): in-sample the churn diagnosis CONFIRMED (maker 24/24 promote at 13–15%/yr @ ~2% vol; taker 15/24) → **HOLDOUT 0/15 PASS: the carry itself was ABSENT in 2025-11→2026-06** (always-in books negative −0.09…−0.23; selective books FLAT the whole window — correct desk behaviour, nothing to promote). 15 reads recorded (taker); the maker sweep was in-sample-only. **THE FREE-DATA CHAPTER IS CLOSED AT EVERY SEAM with honest instruments — direction, re-rank carry, low-churn carry; the carry is real but regime-conditional, and the current regime has none.** THE STRATEGIC PICK ALSO LANDED SAME-DAY: **[You] chose OPTIONS** → the M5.5 research on-ramp is LIVE — #150 merged (the NSE bhavcopy data layer; review caught the **P&L-critical expiry-day settle trap**, now fenced in the docstrings) and **8.9M contract-days of free NIFTY/BANKNIFTY chains ingested (2016→2026-06)** — see the M5.5 bullet in 🟡 carried. M5.5b IS ALSO DONE (#152 built → merged; #153 the era fix — NSE's expiry-day settle column was **0 through 2020-01** and the underlying's final level only from 2020-02, found by leg-level attribution of an impossible bleed and fixed era-proof via the median expiry-parity `settlement_level`; no holdout was ever read through the buggy fold): **the corrected first options verdict = NIFTY 0/12 + BANKNIFTY 0/12 in-sample condors — ZERO options-holdout reads spent, the fresh ~2.1y holdout (2024-05-24→2026-06-30) stays VIRGIN.** Diagnosis: BANKNIFTY weeklies mildly positive (best in-sample OOS ann Sharpe 0.58), NIFTY mildly negative — the premium is real but the retail-simplest EOD hold-to-expiry condor doesn't clear the Indian cost stack at DSR significance. ⏳ THE LIVE DECISION = [You]: the options machinery is now proven end-to-end and each NEW pre-registered family costs ~an hour (expiry-day-only structures / vol-regime-conditioned entries / delta-hedged premium / broken-wing shapes / BANKNIFTY-weekly refinements — the virgin holdout waits for the first in-sample survivor), or pause options with a clean powered first read. Optional cheap build still on offer: the **funding-regime tripwire** (basis-desk re-entry alert; monitoring only). [You] ops one-liners staged in the *part 2* bullet's ops note (remote-branch deletion — classifier-blocked for CC; box-timer disable — SSH unreachable from this network). Deploy gates unchanged everywhere ([You]: SEBI/venue/CA; basis: real venue funding accrual in the worker before any paper/live). The next *phase* (Phase 4, first real money) stays fully [You]-gated:**
1. **M3.0 edge-hunt is DONE — free-data has NO holdout-surviving edge across price AND funding (2026-06-29/30).** The first verdict (price-only TA, single-instrument): 3 sweeps → 7 in-sample survivors, 0 hold out. Then the [You] strategic pick was TAKEN this session (2026-06-30): **cross-sectional rank** (#138/#139/#140) — momentum was robustly in-sample-positive but **holdout-REJECTED** (sign-reversed), reversal found nothing; and **funding carry** (#141/#142) — positive-but-marginal in-sample, **0 survivors** (DSR 0.810 < 0.92; price risk dilutes a perp-only carry). OI ruled out (free 30-day cap). The discovery→rigor→holdout machine + all panel/funding infra are **built + validated**; the bottleneck is conclusively the **data/market-efficiency**, not the strategy class. **➡️ THE LIVE DECISION is now [You]'s STRATEGIC FORK toward a bigger INPUT:** (1) **delta-neutral BASIS trade** (perp vs spot — isolates the carry from price risk; spot is FREE, a 2-leg build; the most targeted cheap step); (2) **paid data** (Tardis order-book/derivs); (3) **less-efficient markets** (small-caps/new-listings/options M5.5); (4) **pause** — the engine's done. *(PR #140 — the holdout-panel infra — was squash-MERGED 2026-07-01 per the 2026-07-02 plan authorization; the fork was resolved as option 1 + a science re-run — see the *part 2* bullet.)* See the M3.0 bullets in 🟡 carried for the full evidence + the `--survivor`/`panel_holdout_review.py` paths (built, waiting for a survivor). *(Operational notes if more free-data runs are wanted: the box has a nightly discovery timer that re-runs the ~2.75 h 9-template×48-cell sweep — verify/trim it; the **1-second** leg needs a bigger research box (#133); the **Kite 1-minute** pull needs a fresh daily 2FA token via `scripts/ingest_kite.py`.)*
2. **Optional polish — DONE this session:** token hot-reload (#121; the laptop relay REMOVED per [You] — the minting source awaits a Lemma service token / box self-mint), the **cockpit Approvals view → `approval_requests`** (#123, deployed — paper + holdout verdict in-app), and the **`risk_events` per-event sync** (#124, kill trips → mission control). **Deferred to M3.0:** the trade-coupled per-event tables (fills/orders/positions/pnl_snapshots — they FK to a deployment the idle soak lacks; exercised by real trading).
3. **Phase 4 (first real money — Delta live, staged) is the next phase + fully [You]-gated** (live keys, static-IP VPS, exchange-side cancel-on-disconnect, the SEBI long-poles; every go-live a fresh human FORM — never CC). See the Phase-4 table + the 🔒 [You]-gated section.

**Lemma context for the next session:** default pod is **Vault** (`019ef606-…`, set via `lemma config set-default-pod`); `[You]` may add `LEMMA_POD_ID` to `.claude/settings.local.json` to scope it per-project (CC is guardrail-blocked from editing that file). desk runs **Opus via `lemma daemon`** (Mac) + the **Claude Code** harness — daemon must be up for desk (NOT the dashboard). Deploy/monitor the worker: rsync `alpha-core`/`worker`/`config`/`deploy` to `~/alpha`, `uv sync --all-packages`, `sudo systemctl …`; `journalctl -u alpha-worker -f`. Box `.env` is **testnet-only**.

**Deferred lifts (rejoin when their layer/phase lands; tracked in the coverage `omit`):** `strategy/registry` + the ~8 example strategies + the **options** layer (now scheduled → **M5.5**, Phase 5); `data.universe` (needs the universe config); `portfolio/setup`. *(The data readers — `normalize`/`historical`/`feed` + `store` — landed in B1a.1a; the `vega`→`alpha` namespacing is done, #27. The fuller `helpers/config.py` is [You]-gated, above.)*
