import { Boxes } from 'lucide-react'
import { ViewStub } from '../ui'

export function Strategies() {
  return (
    <ViewStub
      icon={Boxes}
      head="Strategies"
      sub="Discovered and deployed strategies with their parameters and lifecycle state, from the strategies + deployments tables."
      milestone="M2.4"
    />
  )
}
